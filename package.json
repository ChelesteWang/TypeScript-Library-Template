{
  "name": "your-js-lib",
  "version": "0.1.0",
  "description": "a TypeScript library cli",
  "main": "./build/your-js-lib.min.js",
  "scripts": {
    "start": "concurrently --names \"ROLLUP,HTTP\" -c \"bgBlue.bold,bgGreen.bold\" \"rollup -c -w -m inline\" \"serve --port 3001\"",
    "lint": "eslint ./src --config=eslintrc.json",
    "build": "rollup -c --pub"
  },
  "authors": [
    "xxxx <xxxxx@gmail.com>"
  ],
  "license": "MIT",
  "devDependencies": {
  }
}
